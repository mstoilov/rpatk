// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("prpa", "for prpa support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("prpa", "enable prpa support", "no");

if (PHP_PRPA != "no") {
	EXTENSION("prpa", "prpa.c");
}

